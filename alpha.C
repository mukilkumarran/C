#include <stdio.h>
main
{
char c;
printf("Enter the charater:");
scanf("%c",&c);
if((c>='a' && c<='z')||(c>='A' && c<='Z')
printf("%c is the alphabet",c);
else
printf("%c is not the alphabet",c);
return 0;
}
